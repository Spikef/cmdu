#!/usr/bin/env node

var app = require('../');

app.version = '0.0.1';

app
    .describe('Application simple description')
    .option('-f, --foo', 'enable some foo')
    .option('-b, --bar', 'enable some bar')
    .option('-B, --baz', 'enable some baz')
    .listen();